metrics_name: sql_server.custom.database_count
monitor_sql: |
    SELECT 
        count(*)
    FROM 
        sys.databases;
monitor_value_position: 0
dd_tags:
    env: prd
    project: zaiba2
    application: mssql