metrics_name: sql_server.custom.objects_count
monitor_sql: |
    SELECT 
        count(*)
    FROM 
        sys.objects;
monitor_value_position: 0
dd_tags:
    env: prd
    project: zaiba2
    application: mssql